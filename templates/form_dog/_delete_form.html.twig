<form method="post" action="{{ path('app_form_dog_delete', {'id': dog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dog.id) }}">
    <button class="btn">Delete</button>
</form>
